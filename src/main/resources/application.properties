# Configura��o de conex�o com o banco de dados MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/sala_facil?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=

# Configura��es JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update  # Em produ��o, substitua por 'validate' ou 'none'
spring.jpa.show-sql=true

# Dialeto do Hibernate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Nome da aplicação
spring.application.name=SalaFacilSpace
